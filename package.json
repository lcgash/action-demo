
{
  "name": "action-demo",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "ncc build main.js -o dist", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
    "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
